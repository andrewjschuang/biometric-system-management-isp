version: '3.1'

services:
  bmsisp:
    image: bmsisp:1.0.2
    restart: always
    environment:
      FLASK_APP: server.py
    ports:
      - 5000:5000
    devices:
      - /dev/video0:/dev/video0
    volumes:
      - bmsisp-volume:/app/found

volumes:
  bmsisp-volume:
